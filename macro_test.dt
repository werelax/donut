(define some (+ 5 5))

(let ((more (* some 10)))
  (defmacro jarl (x)
    (cons 'quote (list 1 2 x))))

(defmacro jorl (y)
  (cons 'quote (list 7 8 y)))


;; Still doesn't know how to bind functions

;(define (jarl a)
;  (console.log a))
