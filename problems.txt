* (define jarl [{}])
  - no compila, se hace un lío parseando {}
  - funciona con (define jarl (list {}))

* (defmacro) en runtime?
  - Por ejemplo, el clisp esto funciona:
    ((lambda () (defmacro jarl (x) `(list ,x))))
  - Pero lo veo bastante chungo de implementar? o no?
  - En realidad, un 'macroexpand' en runtime es lo mismo que una función?
  - Pero cómo vas a poder modificar syntaxis si el programa ya está compilado??
  - Esto son rollos meta-circulares del interprete-compiler de LISP...
  * Por ahora lo dejo fuera!
